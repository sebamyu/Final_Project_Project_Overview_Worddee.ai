// 📦 นำเข้า Type "NextConfig" เพื่อให้ TypeScript ช่วยเช็คว่าเราตั้งค่าถูกไหม
import type { NextConfig } from "next";

// ⚙️ ตัวแปรเก็บการตั้งค่าทั้งหมด
const nextConfig: NextConfig = {
  /* config options here */
  
  // ตัวอย่างการตั้งค่าที่นิยมใช้ (ตอนนี้ยังว่างอยู่ แต่คุณอาจต้องใช้ในอนาคต):
  
  // 1. อนุญาตให้โหลดรูปภาพจากเว็บอื่น (เช่น Unsplash ที่เราใช้ในโปรเจกต์)
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'images.unsplash.com', // อนุญาตโดเมนนี้
  //     },
  //   ],
  // },

  // 2. ปิด Strict Mode (ถ้าไม่อยากให้ useEffect รัน 2 รอบตอน Dev)
  // reactStrictMode: false, 

  // 3. ตั้งค่า Redirect (เช่น พิมพ์ /old-page ให้เด้งไป /new-page)
  // async redirects() {
  //   return [
  //     {
  //       source: '/old',
  //       destination: '/new',
  //       permanent: true,
  //     },
  //   ];
  // },
};

// 📤 ส่งออกการตั้งค่านี้ให้ Next.js นำไปใช้ตอนเริ่มระบบ
export default nextConfig;
