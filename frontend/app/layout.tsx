// 📦 1. ส่วนนำเข้า (Imports)
import type { Metadata } from "next"; // นำเข้า Type สำหรับกำหนด Metadata (SEO)
import { Geist, Geist_Mono } from "next/font/google"; // ดึงฟอนต์ Geist จาก Google ผ่านระบบของ Next.js (โหลดเร็วมาก)
import "./globals.css"; // นำเข้าไฟล์ CSS หลัก (ที่มี Tailwind) ให้มีผลทั้งเว็บ

// 🎨 2. การตั้งค่าฟอนต์ (Font Configuration)
// Next.js จะโหลดฟอนต์นี้มาเก็บไว้ในเครื่องผู้ใช้เลย (Self-hosted) ไม่ต้องดึงจาก Google ทุกครั้ง ทำให้เว็บโหลดไว
const geistSans = Geist({
  variable: "--font-geist-sans", // ตั้งชื่อตัวแปร CSS เพื่อเอาไปใช้ใน Tailwind
  subsets: ["latin"],            // โหลดเฉพาะตัวอักษรภาษาอังกฤษ/ละติน (ช่วยลดขนาดไฟล์)
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono", // ฟอนต์แบบ Monospace (ตัวกว้างเท่ากัน) สำหรับแสดง Code
  subsets: ["latin"],
});

// 🌍 3. การตั้งค่า SEO (Metadata)
// ข้อมูลตรงนี้จะไปโผล่ใน <head> ของ HTML (Title bar, Description ใน Google)
export const metadata: Metadata = {
  title: "Create Next App",       // ชื่อเว็บที่จะขึ้นบน Tab Browser
  description: "Generated by create next app", // คำอธิบายเว็บเวลาแชร์หรือค้นหา
};

// 🏗️ 4. ส่วนประกอบหลัก (Root Layout Component)
// ฟังก์ชันนี้คือ "กรอบ" ที่จะห่อหุ้มทุกหน้าในเว็บเรา
export default function RootLayout({
  children, // "ลูก" คือเนื้อหาของแต่ละหน้า (เช่น หน้า Dashboard, หน้า Word of the Day)
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    // กำหนดภาษาหลักของเว็บเป็นภาษาอังกฤษ
    <html lang="en">
      <body
        // 🖌️ ใส่ Class CSS ที่ body เพื่อให้ทุกส่วนในเว็บได้รับผล
        // 1. ${geistSans.variable}: ฉีดตัวแปรฟอนต์ Sans เข้าไป
        // 2. ${geistMono.variable}: ฉีดตัวแปรฟอนต์ Mono เข้าไป
        // 3. antialiased: (Tailwind) ทำให้ตัวหนังสือเรียบเนียน คมชัดขึ้น
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {/* 👇 เนื้อหาของแต่ละหน้า (Page) จะถูกนำมาแปะตรงนี้ 👇 */}
        {children}
      </body>
    </html>
  );
}

