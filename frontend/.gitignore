# --- คำอธิบายส่วนหัว ---
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# --- 📦 Dependencies (ไลบรารีต่างๆ) ---
# node_modules: เป็นโฟลเดอร์ที่เก็บโค้ดของคนอื่นที่เราโหลดมา (ใหญ่มากและไฟล์เยอะมหาศาล)
# ไม่ต้องอัปโหลด เพราะเราสามารถสั่ง `npm install` เพื่อโหลดใหม่ได้เสมอจาก package.json
/node_modules

# .pnp: ไฟล์ที่เกี่ยวข้องกับ Yarn Plug'n'Play (ระบบจัดการแพ็กเกจแบบใหม่)
/.pnp
.pnp.*

# .yarn/*: ไฟล์แคชของ Yarn
.yarn/*
# เครื่องหมาย ! หมายถึง "ยกเว้น" (คือให้เอาไฟล์พวกนี้ขึ้น GitHub ได้)
!.yarn/patches  # แพทช์แก้ไขไลบรารี
!.yarn/plugins  # ปลั๊กอินเสริมของ Yarn
!.yarn/releases # เวอร์ชันของ Yarn
!.yarn/versions # ประวัติเวอร์ชัน

# --- 🧪 Testing (การทดสอบ) ---
# /coverage: รายงานผลการทดสอบว่าโค้ดเราถูกเทสต์ไปกี่เปอร์เซ็นต์ (เป็นไฟล์ HTML ที่สร้างใหม่ได้ตลอด)
/coverage

# --- ⚡ Next.js (ไฟล์ที่เกิดจากการ Build) ---
# /.next/: โฟลเดอร์ที่ Next.js สร้างขึ้นตอนเรารัน `npm run dev` หรือ `build`
# มันคือผลลัพธ์การแปลงโค้ด ไม่ใช่โค้ดต้นฉบับ จึงไม่ต้องเก็บ
/.next/
/out/

# --- 🚀 Production (ไฟล์สำหรับใช้งานจริง) ---
# /build: โฟลเดอร์ที่ได้จากการ build โปรเจกต์ (บาง Framework ใช้ชื่อนี้)
/build

# --- 🗑️ Misc (ไฟล์ขยะของระบบ) ---
# .DS_Store: ไฟล์ขยะที่เครื่อง Mac สร้างขึ้นอัตโนมัติ (เอาไว้จำตำแหน่งไอคอน) ไม่เกี่ยวกับโปรเจกต์เลย
.DS_Store
# *.pem: กุญแจเข้ารหัส (SSL Certificate) **ห้ามเอาขึ้นเด็ดขาด** เพื่อความปลอดภัย
*.pem

# --- 🐛 Debug (ไฟล์แจ้งเตือนข้อผิดพลาด) ---
# ไฟล์ log ต่างๆ ที่เกิดขึ้นตอนโปรแกรมพัง ไม่จำเป็นต้องเก็บ
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- 🔐 Env Files (ไฟล์ความลับ) ---
# .env*: ไฟล์เก็บรหัสผ่าน API Key หรือ Database Password
# **สำคัญที่สุด** ห้ามเอาขึ้น GitHub เด็ดขาด ไม่งั้นโดนแฮกได้ง่ายๆ
.env*

# --- ▲ Vercel (ไฟล์ตั้งค่าของ Cloud) ---
# .vercel: โฟลเดอร์ที่ Vercel CLI สร้างขึ้นตอน deploy
.vercel

# --- 📘 TypeScript (ไฟล์กำหนดค่า TS) ---
# *.tsbuildinfo: ไฟล์ cache ของ TypeScript ช่วยให้ compile เร็วขึ้น
*.tsbuildinfo
# next-env.d.ts: ไฟล์ที่ Next.js สร้างให้อัตโนมัติเพื่อรองรับ TypeScript ไม่ต้องแก้เอง
next-env.d.ts
